#!/usr/bin/env python3
"""
precond_v3 sweep utilities.

This script keeps ORCD experiments reproducible by rendering one sbatch file per
run from a declarative YAML spec. Each run freezes its resolved config into its
own output directory via train.py.
"""

from __future__ import annotations

import argparse
import csv
import json
import shlex
import subprocess
import sys
from pathlib import Path
from typing import Any

import yaml

ROOT = Path(__file__).resolve().parents[3]
DEFAULT_SPEC = ROOT / "experiments" / "claude" / "precond_v3" / "sweeps" / "north_star_up_20260501.yaml"
CONFIG_DIR = ROOT / "experiments" / "claude" / "precond_v3" / "configs"
GENERATED_DIR = ROOT / "experiments" / "claude" / "precond_v3" / "launch" / "generated"


def load_spec(path: Path) -> dict[str, Any]:
    with path.open() as f:
        spec = yaml.safe_load(f) or {}
    required = ["sweep_id", "root_outdir", "pairs", "experiments", "launcher"]
    missing = [key for key in required if key not in spec]
    if missing:
        raise ValueError(f"Missing required spec keys: {missing}")
    return spec


def merge_args(defaults: dict[str, Any], overrides: dict[str, Any]) -> dict[str, Any]:
    out = dict(defaults)
    out.update(overrides or {})
    return out


def cli_args_from_mapping(args: dict[str, Any]) -> str:
    parts: list[str] = []
    for key, value in args.items():
        flag = "--" + key
        if isinstance(value, bool):
            if value:
                parts.append(flag)
        elif value is None:
            continue
        else:
            parts.extend([flag, str(value)])
    return " ".join(shlex.quote(p) for p in parts)


def pair_config(pair: str) -> str:
    path = CONFIG_DIR / f"pair_{pair}.yaml"
    if not path.exists():
        raise FileNotFoundError(path)
    return str(path.relative_to(ROOT))


def experiment_outdir(spec: dict[str, Any], exp: dict[str, Any]) -> str:
    outdir = exp.get("outdir")
    if outdir == "current":
        return spec.get("current_outdir", "/orcd/scratch/orcd/006/fkiewiet/freq2transfer/precond_v3_runs")
    if outdir:
        return str(outdir)
    return str(Path(spec["root_outdir"]) / exp["id"])


def render_one(spec: dict[str, Any], exp: dict[str, Any], pair: str, outdir: Path) -> Path:
    defaults = spec.get("defaults", {})
    resources = spec.get("resources", {})
    args = merge_args(defaults.get("args", {}), exp.get("args", {}))
    extra_args = cli_args_from_mapping(args)

    run_id = f"{spec['sweep_id']}__{exp['id']}__{pair}"
    job_name = run_id[-120:]
    script_path = outdir / f"{run_id}.sbatch"

    launcher = spec["launcher"]
    log_dir = spec.get("log_dir", "/orcd/scratch/orcd/006/fkiewiet/freq2transfer/slurm_logs")
    direction = exp.get("direction", defaults.get("direction", "up"))
    max_runtime_h = exp.get("max_runtime_h", defaults.get("max_runtime_h", 11.5))
    num_workers = exp.get("num_workers", defaults.get("num_workers", 0))
    exp_outdir = experiment_outdir(spec, exp)
    target_epochs = expected_epochs(spec, exp)
    auto_resubmit = exp.get("auto_resubmit", defaults.get("auto_resubmit", True))
    max_auto_resubmits = exp.get("max_auto_resubmits", defaults.get("max_auto_resubmits", 20))
    script_rel = script_path.relative_to(ROOT)

    body = f"""#!/bin/bash
# Auto-generated by experiments/claude/precond_v3/sweep.py
# Sweep: {spec['sweep_id']}
# Experiment: {exp['id']}
# Pair: {pair}
# Hypothesis: {exp.get('hypothesis', '')}
#SBATCH --job-name={job_name}
#SBATCH --output={log_dir}/%x_%j.log
#SBATCH --error={log_dir}/%x_%j.err
#SBATCH --time={resources.get('time', '12:00:00')}
#SBATCH --partition={resources.get('partition', 'mit_preemptable')}
#SBATCH --gres={resources.get('gres', 'gpu:1')}
#SBATCH --cpus-per-task={resources.get('cpus_per_task', 4)}
#SBATCH --mem={resources.get('mem', '64G')}
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --signal=B:USR1@300

set -euo pipefail

ROOT="${{SLURM_SUBMIT_DIR:-$HOME/Freq2Transfer}}"
cd "$ROOT"

export CONFIG={shlex.quote(pair_config(pair))}
export DIRECTION={shlex.quote(str(direction))}
export MAX_RUNTIME_H={shlex.quote(str(max_runtime_h))}
export NUM_WORKERS={shlex.quote(str(num_workers))}
export EXPERIMENT_OUTDIR={shlex.quote(exp_outdir)}
export EXTRA_ARGS={shlex.quote(extra_args)}
PAIR={shlex.quote(pair)}
RUN_DIR="$EXPERIMENT_OUTDIR/pair_$PAIR/T_$DIRECTION"
SELF_SCRIPT="$ROOT/{script_rel}"
TARGET_EPOCHS={shlex.quote(str(target_epochs))}
AUTO_RESUBMIT={1 if auto_resubmit else 0}
MAX_AUTO_RESUBMITS={shlex.quote(str(max_auto_resubmits))}
AUTO_RESUBMIT_COUNT_FILE="$RUN_DIR/.auto_resubmit_count"

summary_field() {{
  local field="$1"
  if [[ ! -f "$RUN_DIR/summary.json" ]]; then
    echo ""
    return 0
  fi
  python3 -c 'import json, sys; p, k = sys.argv[1:3]; print(json.load(open(p)).get(k, ""))' "$RUN_DIR/summary.json" "$field" || true
}}

auto_resubmit_if_needed() {{
  local trigger="$1"
  local status
  local last_epoch
  local target_last
  local count

  if [[ "$AUTO_RESUBMIT" != "1" ]]; then
    echo "[auto-resubmit] disabled"
    return 0
  fi

  mkdir -p "$RUN_DIR"
  status="$(summary_field stopped_reason)"
  last_epoch="$(summary_field last_epoch)"
  if [[ -z "$last_epoch" ]]; then
    last_epoch=-1
  fi
  target_last=$((TARGET_EPOCHS - 1))

  if [[ "$status" == "max_epochs" && "$last_epoch" -ge "$target_last" ]]; then
    echo "[auto-resubmit] complete: status=$status last_epoch=$last_epoch target_last=$target_last"
    return 0
  fi

  if [[ "$trigger" == "post_run" && "$status" != "runtime_cap" ]]; then
    echo "[auto-resubmit] not resubmitting after normal exit: status=${{status:-missing}} last_epoch=$last_epoch"
    return 0
  fi

  count=0
  if [[ -f "$AUTO_RESUBMIT_COUNT_FILE" ]]; then
    count="$(cat "$AUTO_RESUBMIT_COUNT_FILE" 2>/dev/null || echo 0)"
  fi
  if (( count >= MAX_AUTO_RESUBMITS )); then
    echo "[auto-resubmit] limit reached: $count / $MAX_AUTO_RESUBMITS"
    return 0
  fi

  count=$((count + 1))
  echo "$count" > "$AUTO_RESUBMIT_COUNT_FILE"
  echo "[auto-resubmit] submitting continuation #$count because trigger=$trigger status=${{status:-missing}} last_epoch=$last_epoch target_last=$target_last"
  sbatch "$SELF_SCRIPT"
}}

on_usr1() {{
  echo "[auto-resubmit] received Slurm USR1 pre-timeout signal at $(date)"
  auto_resubmit_if_needed "slurm_signal"
  exit 143
}}
trap on_usr1 USR1

if [[ -f "$RUN_DIR/last.pt" && "$EXTRA_ARGS" == *"--fresh"* ]]; then
  echo "Existing checkpoint found at $RUN_DIR/last.pt"
  echo "Removing --fresh so this submission resumes instead of restarting."
  EXTRA_ARGS="${{EXTRA_ARGS//--fresh/}}"
fi

echo "run_id          : {run_id}"
echo "hypothesis      : {exp.get('hypothesis', '')}"
echo "config          : $CONFIG"
echo "direction       : $DIRECTION"
echo "experiment root : $EXPERIMENT_OUTDIR"
echo "extra args      : $EXTRA_ARGS"
echo "auto resubmit   : $AUTO_RESUBMIT (max $MAX_AUTO_RESUBMITS)"
echo

set +e
bash {shlex.quote(launcher)}
train_rc=$?
set -e
trap - USR1

if [[ "$train_rc" -ne 0 ]]; then
  echo "[auto-resubmit] launcher failed with exit code $train_rc; not resubmitting automatically."
  exit "$train_rc"
fi

auto_resubmit_if_needed "post_run"
"""
    script_path.write_text(body)
    script_path.chmod(0o755)
    return script_path


def render(spec_path: Path) -> list[Path]:
    spec = load_spec(spec_path)
    outdir = GENERATED_DIR / spec["sweep_id"]
    outdir.mkdir(parents=True, exist_ok=True)

    scripts = []
    for exp in spec["experiments"]:
        for pair in spec["pairs"]:
            scripts.append(render_one(spec, exp, pair, outdir))

    manifest = outdir / "submit_all.sh"
    lines = ["#!/bin/bash", "set -euo pipefail", ""]
    for script in scripts:
        lines.append(f"sbatch {shlex.quote(str(script.relative_to(ROOT)))}")
    manifest.write_text("\n".join(lines) + "\n")
    manifest.chmod(0o755)
    return scripts


def submit(scripts: list[Path], dry_run: bool) -> None:
    for script in scripts:
        cmd = ["sbatch", str(script)]
        if dry_run:
            print(" ".join(shlex.quote(c) for c in cmd))
        else:
            subprocess.run(cmd, check=True)


def read_log_best(log_path: Path) -> dict[str, str] | None:
    if not log_path.exists():
        return None
    with log_path.open() as f:
        rows = list(csv.DictReader(f))
    if not rows:
        return None
    return min(rows, key=lambda r: float(r["val_loss"]))


def summarize(spec_path: Path, markdown: bool = False) -> None:
    rows = collect_rows(spec_path)
    headers = ["experiment", "pair", "status", "last_epoch", "best_epoch", "best_val", "last_val", "test_at_best"]
    if markdown:
        print("| " + " | ".join(headers) + " |")
        print("| " + " | ".join(["---"] * len(headers)) + " |")
        for row in rows:
            print("| " + " | ".join(str(row[h]) for h in headers) + " |")
    else:
        writer = csv.DictWriter(sys.stdout, fieldnames=headers + ["outdir"])
        writer.writeheader()
        writer.writerows(rows)


def collect_rows(spec_path: Path) -> list[dict[str, Any]]:
    spec = load_spec(spec_path)
    rows: list[dict[str, Any]] = []
    for exp in spec["experiments"]:
        exp_outdir = Path(experiment_outdir(spec, exp))
        for pair in spec["pairs"]:
            d = exp_outdir / f"pair_{pair}" / "T_up"
            summary_path = d / "summary.json"
            log_path = d / "log.csv"
            summary = {}
            if summary_path.exists():
                summary = json.loads(summary_path.read_text())
            best_csv = read_log_best(log_path)
            rows.append({
                "experiment": exp["id"],
                "pair": pair,
                "status": summary.get("stopped_reason", "missing"),
                "last_epoch": summary.get("last_epoch", ""),
                "best_epoch": summary.get("best_epoch", best_csv.get("epoch") if best_csv else ""),
                "best_val": summary.get("best_val_loss", best_csv.get("val_loss") if best_csv else ""),
                "last_val": summary.get("last_val_loss", ""),
                "test_at_best": summary.get("test_loss_at_best", ""),
                "outdir": str(d),
            })
    return rows


def expected_epochs(spec: dict[str, Any], exp: dict[str, Any]) -> int:
    defaults = spec.get("defaults", {}).get("args", {})
    args = merge_args(defaults, exp.get("args", {}))
    return int(args.get("epochs", 500))


def incomplete_scripts(spec_path: Path) -> list[Path]:
    spec = load_spec(spec_path)
    generated = GENERATED_DIR / spec["sweep_id"]
    scripts = []
    for exp in spec["experiments"]:
        exp_outdir = Path(experiment_outdir(spec, exp))
        target_last = expected_epochs(spec, exp) - 1
        for pair in spec["pairs"]:
            summary_path = exp_outdir / f"pair_{pair}" / "T_up" / "summary.json"
            incomplete = True
            if summary_path.exists():
                summary = json.loads(summary_path.read_text())
                last_epoch = int(summary.get("last_epoch", -1))
                stopped_reason = summary.get("stopped_reason", "")
                incomplete = last_epoch < target_last or stopped_reason == "runtime_cap"
            if incomplete:
                scripts.append(generated / f"{spec['sweep_id']}__{exp['id']}__{pair}.sbatch")
    return scripts


def resume_incomplete(spec_path: Path, dry_run: bool) -> None:
    render(spec_path)
    scripts = incomplete_scripts(spec_path)
    if not scripts:
        print("No incomplete runs found.")
        return
    submit(scripts, dry_run=dry_run)


def _float_or_nan(value: Any) -> float:
    try:
        if value == "":
            return float("nan")
        return float(value)
    except (TypeError, ValueError):
        return float("nan")


def plot(spec_path: Path) -> None:
    import math
    import matplotlib
    matplotlib.use("Agg")
    import matplotlib.pyplot as plt

    spec = load_spec(spec_path)
    rows = collect_rows(spec_path)
    plot_dir = Path(spec["root_outdir"]) / "_plots"
    plot_dir.mkdir(parents=True, exist_ok=True)

    pairs = list(spec["pairs"])
    experiments = [exp["id"] for exp in spec["experiments"]]

    # Bar plot: best validation loss by pair and experiment.
    fig_w = max(10, 1.2 * len(experiments))
    fig, axes = plt.subplots(1, len(pairs), figsize=(fig_w, 4.2), sharey=False)
    if len(pairs) == 1:
        axes = [axes]
    for ax, pair in zip(axes, pairs):
        vals = []
        labels = []
        for exp in experiments:
            row = next((r for r in rows if r["pair"] == pair and r["experiment"] == exp), None)
            vals.append(_float_or_nan(row["best_val"]) if row else float("nan"))
            labels.append(exp)
        ax.bar(range(len(vals)), vals, color="#3b82f6")
        ax.set_title(f"{pair.replace('_', '→')} best val")
        ax.set_xticks(range(len(labels)))
        ax.set_xticklabels(labels, rotation=60, ha="right", fontsize=7)
        ax.set_ylabel("best validation loss")
        ax.grid(axis="y", alpha=0.25)
    fig.tight_layout()
    fig.savefig(plot_dir / "best_val_by_pair.png", dpi=180)
    plt.close(fig)

    # Curves: validation loss histories by pair.
    for pair in pairs:
        fig, ax = plt.subplots(figsize=(8.5, 5.0))
        any_curve = False
        for exp in spec["experiments"]:
            d = Path(experiment_outdir(spec, exp)) / f"pair_{pair}" / "T_up"
            log_path = d / "log.csv"
            if not log_path.exists():
                continue
            with log_path.open() as f:
                log_rows = list(csv.DictReader(f))
            if not log_rows:
                continue
            xs = [int(r["epoch"]) for r in log_rows]
            ys = [float(r["val_loss"]) for r in log_rows]
            ax.plot(xs, ys, lw=1.8, label=exp["id"])
            any_curve = True
        if any_curve:
            ax.set_title(f"{pair.replace('_', '→')} validation curves")
            ax.set_xlabel("epoch")
            ax.set_ylabel("validation loss")
            ax.set_yscale("log")
            ax.grid(True, which="both", alpha=0.25)
            ax.legend(fontsize=7)
            fig.tight_layout()
            fig.savefig(plot_dir / f"val_curves_pair_{pair}.png", dpi=180)
        plt.close(fig)

    # Progress plot: last epoch reached, useful for runtime-cap resumes.
    fig, ax = plt.subplots(figsize=(max(10, 0.55 * len(rows)), 4.2))
    labels = [f"{r['experiment']}\n{r['pair']}" for r in rows]
    ys = [_float_or_nan(r["last_epoch"]) for r in rows]
    colors = ["#16a34a" if r["status"] == "max_epochs" else "#f59e0b" if r["status"] == "runtime_cap" else "#64748b" for r in rows]
    ax.bar(range(len(rows)), ys, color=colors)
    ax.set_xticks(range(len(rows)))
    ax.set_xticklabels(labels, rotation=75, ha="right", fontsize=6)
    ax.set_ylabel("last epoch")
    ax.set_title("Sweep progress")
    ax.grid(axis="y", alpha=0.25)
    fig.tight_layout()
    fig.savefig(plot_dir / "progress_last_epoch.png", dpi=180)
    plt.close(fig)

    # Stability plot: best vs final validation loss.
    fig, ax = plt.subplots(figsize=(max(10, 0.55 * len(rows)), 4.2))
    xs = list(range(len(rows)))
    best_vals = [_float_or_nan(r["best_val"]) for r in rows]
    last_vals = [_float_or_nan(r["last_val"]) for r in rows]
    ax.bar([x - 0.18 for x in xs], best_vals, width=0.36, color="#2563eb", label="best val")
    ax.bar([x + 0.18 for x in xs], last_vals, width=0.36, color="#f97316", label="last val")
    ax.set_xticks(xs)
    ax.set_xticklabels(labels, rotation=75, ha="right", fontsize=6)
    ax.set_ylabel("validation loss")
    ax.set_yscale("log")
    ax.set_title("Best vs final validation loss")
    ax.grid(axis="y", which="both", alpha=0.25)
    ax.legend()
    fig.tight_layout()
    fig.savefig(plot_dir / "best_vs_last_val.png", dpi=180)
    plt.close(fig)

    print(f"Wrote plots to {plot_dir}")


def dataset_diagnostics(spec_path: Path, max_samples: int = 256) -> None:
    spec = load_spec(spec_path)
    outdir = Path(spec["root_outdir"]) / "_diagnostics"
    try:
        outdir.mkdir(parents=True, exist_ok=True)
    except PermissionError:
        outdir = GENERATED_DIR / spec["sweep_id"] / "diagnostics"
        outdir.mkdir(parents=True, exist_ok=True)
        print(f"ORCD output root unavailable here; writing diagnostics to {outdir}")
    pair_to_idx = {"16_32": 0, "32_64": 1, "64_128": 2}

    ds_candidates = [
        Path("/orcd/pool/006/fkiewiet/freq2transfer/datasets_N9600/up_N9600_seed42"),
        Path("/tmp/fkiewiet/datasets_N9600/up_N9600_seed42"),
        ROOT / "experiments" / "claude" / "datasets_persistent" / "up_N9600_seed42",
        ROOT / "experiments" / "claude" / "datasets" / "up_N9600_seed42",
    ]
    ds = next((p for p in ds_candidates if (p / "metadata.json").exists()), None)
    if ds is None:
        searched = "\n".join(f"  - {p}" for p in ds_candidates)
        raise FileNotFoundError(f"Could not find up_N9600_seed42 metadata.json. Searched:\n{searched}")

    for pair in spec["pairs"]:
        pair_idx = pair_to_idx[str(pair)]
        outfile = outdir / f"dataset_scales_pair_{pair}.txt"
        cmd = [
            sys.executable,
            str(ROOT / "experiments" / "claude" / "precond_v3" / "inspect_dataset_scales.py"),
            "--ds_dir", str(ds),
            "--pair_idx", str(pair_idx),
            "--max_samples", str(max_samples),
        ]
        print(" ".join(shlex.quote(c) for c in cmd))
        with outfile.open("w") as f:
            subprocess.run(cmd, check=True, stdout=f, stderr=subprocess.STDOUT)
        print(f"Wrote {outfile}")


def render_benchmarks(spec_path: Path, n_problems: int, n_iters: int, seed: int, device: str) -> list[Path]:
    spec = load_spec(spec_path)
    outdir = GENERATED_DIR / spec["sweep_id"] / "benchmarks"
    outdir.mkdir(parents=True, exist_ok=True)
    scripts: list[Path] = []

    resources = spec.get("resources", {})
    log_dir = spec.get("log_dir", "/orcd/scratch/orcd/006/fkiewiet/freq2transfer/slurm_logs")
    for exp in spec["experiments"]:
        exp_outdir = Path(experiment_outdir(spec, exp))
        for pair in spec["pairs"]:
            ckpt = exp_outdir / f"pair_{pair}" / "T_up" / "best.pt"
            result_dir = Path(spec["root_outdir"]) / "_benchmarks" / exp["id"] / f"pair_{pair}"
            run_id = f"{spec['sweep_id']}__bench__{exp['id']}__{pair}"
            script = outdir / f"{run_id}.sbatch"
            body = f"""#!/bin/bash
# Auto-generated benchmark job by experiments/claude/precond_v3/sweep.py
#SBATCH --job-name={run_id[-120:]}
#SBATCH --output={log_dir}/%x_%j.log
#SBATCH --error={log_dir}/%x_%j.err
#SBATCH --time={resources.get('time', '12:00:00')}
#SBATCH --partition={resources.get('partition', 'mit_preemptable')}
#SBATCH --gres={resources.get('gres', 'gpu:1')}
#SBATCH --cpus-per-task={resources.get('cpus_per_task', 4)}
#SBATCH --mem={resources.get('mem', '64G')}
#SBATCH --nodes=1
#SBATCH --ntasks=1

set -euo pipefail
ROOT="${{SLURM_SUBMIT_DIR:-$HOME/Freq2Transfer}}"
cd "$ROOT"
if [ -f "$ROOT/.venv/bin/activate" ]; then
  source "$ROOT/.venv/bin/activate"
fi

CKPT={shlex.quote(str(ckpt))}
OUTDIR={shlex.quote(str(result_dir))}

if [ ! -f "$CKPT" ]; then
  echo "Missing checkpoint: $CKPT"
  exit 2
fi

python3 experiments/claude/benchmark_warmstart_unet.py \\
  --ckpt "$CKPT" \\
  --device {shlex.quote(device)} \\
  --outdir "$OUTDIR" \\
  --n_problems {int(n_problems)} \\
  --n_iters {int(n_iters)} \\
  --seed {int(seed)}
"""
            script.write_text(body)
            script.chmod(0o755)
            scripts.append(script)

    manifest = outdir / "submit_all_benchmarks.sh"
    lines = ["#!/bin/bash", "set -euo pipefail", ""]
    for script in scripts:
        lines.append(f"sbatch {shlex.quote(str(script.relative_to(ROOT)))}")
    manifest.write_text("\n".join(lines) + "\n")
    manifest.chmod(0o755)
    print(f"Rendered {len(scripts)} benchmark scripts under {outdir}")
    return scripts


def main() -> None:
    p = argparse.ArgumentParser(description="Render, submit, and summarize precond_v3 sweeps.")
    p.add_argument("--spec", type=Path, default=DEFAULT_SPEC)
    sub = p.add_subparsers(dest="cmd", required=True)

    p_render = sub.add_parser("render", help="Render sbatch files from the spec.")
    p_render.add_argument("--submit", action="store_true", help="Submit after rendering.")
    p_render.add_argument("--dry-run", action="store_true", help="Print sbatch commands instead of submitting.")

    p_submit = sub.add_parser("submit", help="Render and submit all jobs.")
    p_submit.add_argument("--dry-run", action="store_true")

    p_sum = sub.add_parser("summarize", help="Summarize completed run directories.")
    p_sum.add_argument("--markdown", action="store_true")

    sub.add_parser("plot", help="Create comparison plots from summary/log files.")

    p_resume = sub.add_parser("resume-incomplete", help="Submit runs that are missing or stopped before the target epoch.")
    p_resume.add_argument("--dry-run", action="store_true")

    p_diag = sub.add_parser("dataset-diagnostics", help="Archive dataset scale diagnostics for all pairs in this spec.")
    p_diag.add_argument("--max-samples", type=int, default=256)

    p_bench = sub.add_parser("render-benchmarks", help="Render post-training warm-start benchmark sbatch files.")
    p_bench.add_argument("--n-problems", type=int, default=5)
    p_bench.add_argument("--n-iters", type=int, default=60)
    p_bench.add_argument("--seed", type=int, default=77777)
    p_bench.add_argument("--device", default="cuda:0")
    p_bench.add_argument("--submit", action="store_true")
    p_bench.add_argument("--dry-run", action="store_true")

    args = p.parse_args()
    if args.cmd == "render":
        scripts = render(args.spec)
        print(f"Rendered {len(scripts)} scripts under {GENERATED_DIR / load_spec(args.spec)['sweep_id']}")
        if args.submit or args.dry_run:
            submit(scripts, dry_run=args.dry_run)
    elif args.cmd == "submit":
        scripts = render(args.spec)
        submit(scripts, dry_run=args.dry_run)
    elif args.cmd == "summarize":
        summarize(args.spec, markdown=args.markdown)
    elif args.cmd == "plot":
        plot(args.spec)
    elif args.cmd == "resume-incomplete":
        resume_incomplete(args.spec, dry_run=args.dry_run)
    elif args.cmd == "dataset-diagnostics":
        dataset_diagnostics(args.spec, max_samples=args.max_samples)
    elif args.cmd == "render-benchmarks":
        scripts = render_benchmarks(args.spec, args.n_problems, args.n_iters, args.seed, args.device)
        if args.submit or args.dry_run:
            submit(scripts, dry_run=args.dry_run)


if __name__ == "__main__":
    main()
